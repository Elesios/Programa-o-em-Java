import java.util.Scanner;



public class Pag130Exer14 {
	public static void main(String[] args) {
		Scanner scan = new Scanner(System.in);
		
		float divida, valor_divida = 0, valor_juros = 0, valor_parcela = 0;
		int parcela = 0, cont = 0;
		
		System.out.println("Digite o Valor da Divida:");
		divida = scan.nextFloat();
		
		for (int i = 0 ; i <= 12; i++) {
			
								
			if (parcela == 0) {
				
				valor_divida = divida;
				valor_juros = divida * 0;
				valor_parcela = valor_divida;
				
				System.out.printf("\n-----------------------------------------------------------------------------------------\n");
				System.out.printf("--Valor da Divida = %.2f",  valor_divida);
				System.out.printf("--Valor do Juros = %.2f",  valor_juros);
				System.out.printf("--Quantidade de Parcelas = %d",  parcela);
				System.out.printf("--Valor Parcela = %.2f",  valor_parcela);
			}else if (parcela == 3) {
				
				valor_juros = divida * 0.10f;
				valor_divida = divida + valor_juros;				
				valor_parcela = valor_divida / parcela;
				
				System.out.printf("\n-----------------------------------------------------------------------------------------\n");
				System.out.printf("--Valor da Divida = %.2f",  valor_divida);
				System.out.printf("--Valor do Juros = %.2f",  valor_juros);
				System.out.printf("--Quantidade de Parcelas = %d",  parcela);
				System.out.printf("--Valor Parcela = %.2f",  valor_parcela);
			}else if (parcela == 6) {
				
				valor_juros = divida * 0.15f;
				valor_divida = divida + valor_juros;				
				valor_parcela = valor_divida / parcela;
				
				System.out.printf("\n-----------------------------------------------------------------------------------------\n");
				System.out.printf("--Valor da Divida = %.2f",  valor_divida);
				System.out.printf("--Valor do Juros = %.2f",  valor_juros);
				System.out.printf("--Quantidade de Parcelas = %d",  parcela);
				System.out.printf("--Valor Parcela = %.2f",  valor_parcela);
			}else if (parcela == 9) {
				
				valor_juros = divida * 0.20f;
				valor_divida = divida + valor_juros;				
				valor_parcela = valor_divida / parcela;
				
				System.out.printf("\n-----------------------------------------------------------------------------------------\n");
				System.out.printf("--Valor da Divida = %.2f",  valor_divida);
				System.out.printf("--Valor do Juros = %.2f",  valor_juros);
				System.out.printf("--Quantidade de Parcelas = %d",  parcela);
				System.out.printf("--Valor Parcela = %.2f",  valor_parcela);
			}else if (parcela == 12) {
				
				valor_juros = divida * 0.25f;
				valor_divida = divida + valor_juros;				
				valor_parcela = valor_divida / parcela;
				
				System.out.printf("\n-----------------------------------------------------------------------------------------\n");
				System.out.printf("--Valor da Divida = %.2f",  valor_divida);
				System.out.printf("--Valor do Juros = %.2f",  valor_juros);
				System.out.printf("--Quantidade de Parcelas = %d",  parcela);
				System.out.printf("--Valor Parcela = %.2f",  valor_parcela);
			}
			
			
			parcela += 3;
			
		}
		
		
		
		
	}

}
